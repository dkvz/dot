# Run `ghostty +show-config --default --docs` to view a list of
# all available config options and their default values.
#
# Additionally, each config option is also explained in detail
# on Ghostty's website, at https://ghostty.org/docs/config.

font-family = CaskaydiaCove Nerd Font
font-size = 16
# I don't know if this does anything lol
bell-features = no-audio,no-title,no-system
theme = dark:Everforest Dark   Hard,light:Rose Pine Dawn
#background-opacity = 0.94
background-image-opacity = 0.06
background-image = ~/.local/share/bg.jpg
background-image-fit = cover
cursor-style-blink = false
window-decoration = none
# Hide the rows x columns thingy that pops up when resizing
resize-overlay = never
# window-padding-x = 2

# There's a bug with increasing/decreasing font size shortcut
# on AZERTY keyboards, currently requires the following:
keybind = ctrl+equal=unbind
keybind = ctrl+==increase_font_size:1
